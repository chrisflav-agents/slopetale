# Session 10 Objectives

## 1. Proetale/Mathlib/RingTheory/Ideal/GoingDown.lean - Complete the Disjoint lemma

**Priority:** HIGHEST - Proof is 90% complete, only one technical lemma remains

**Current state:**
- File has 1 sorry at line 42 (inside the Disjoint proof)
- File does NOT compile (3 type class synthesis errors will resolve once sorry is filled)
- Mathematical strategy is correct and follows blueprint (Stacks 00EA)
- Prover has correctly implemented `IsLocalization.of_le` approach

**What's done:**
- ✅ Correct use of `IsLocalization.of_le` with explicit type parameters
- ✅ Going-down + uniqueness argument setup
- ✅ Identified correct lemmas (`Ideal.exists_le_prime_disjoint`, etc.)

**What remains:**
- ❌ Prove `Disjoint (Ideal.span {n}) (algebraMapSubmonoid S p.primeCompl)` at line 42

**Task:** Fill the sorry at line 42 by proving the Disjoint property. Try these approaches in order:

1. **Search for existing Disjoint lemmas** - Use `lean_leansearch` to find lemmas about:
   - "ideal span disjoint from submonoid"
   - "element not in submonoid closure implies disjoint"
   - "algebraMapSubmonoid disjoint from ideal"

2. **Try IsLocalization.of_le_of_exists_dvd** - This alternative characterization might avoid the Disjoint proof entirely. Search for this lemma and see if it applies.

3. **Direct Disjoint proof** - If no existing lemma works, prove it directly:
   - Show if `a * n ∈ algebraMapSubmonoid S p.primeCompl` for some `a`, derive contradiction
   - Use that `algebraMapSubmonoid S p.primeCompl` is generated by image of `p.primeCompl`
   - Key insight: if `n` is in the submonoid, it contradicts our assumption

4. **Alternative IsLocalization construction** - If all else fails, directly construct the `IsLocalization` instance by proving `map_units`, `surj`, and `exists_of_eq` properties.

**Estimated effort:** 10-30 lines

**Success criteria:**
- Sorry at line 42 is filled
- File compiles with no errors
- No new sorries introduced
