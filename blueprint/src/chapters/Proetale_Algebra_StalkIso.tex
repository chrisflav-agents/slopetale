\chapter{Algebras and ring homomorphisms bijective on stalks}
\label{ch:Proetale_Algebra_StalkIso}

% NOTE: minimal stub created by prover (iter-103) at dispatch's
% explicit instruction because the file did not yet exist. Plan
% agent: please flesh out informal prose. The single declaration
% currently shipped from `Proetale/Algebra/StalkIso.lean` that
% needs informal coverage is `RingHom.BijectiveOnStalks.prod`,
% which is PARKED-unsound at the current signature (the missing
% hypotheses `(hf : f.BijectiveOnStalks)` and
% `(hg : g.BijectiveOnStalks)` were dropped in a refactor and the
% statement is false without them — see counterexample in
% `Proetale/Algebra/StalkIso.lean` L313-316 and recomputation in
% `.archon/task_results/Algebra_StalkIso.lean.md`).
